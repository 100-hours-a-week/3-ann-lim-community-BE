spring:
  datasource:
    url: jdbc:mysql://mysql:3306/community?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: eunseo
    password: eunseo1018@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    property-naming-strategy: SNAKE_CASE

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  profiles:
    active: dev

frontend:
  base-url: https://community-ktb.online